// TODO: Implement zkLogin and profile management logic
export const useAuth = () => {
  const login = () => {
    // Connect to wallet, handle zkLogin
  };

  const logout = () => {
    // Clear session
  };

  return { login, logout };
};
